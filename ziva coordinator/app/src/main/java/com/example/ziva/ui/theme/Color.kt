package com.example.ziva.ui.theme

import androidx.compose.ui.graphics.Color

// ================================
// ZIVA LIGHT THEME
// ================================

// Primary — main ZIVA brand color
val Primary = Color(0xFFB71C1C)

// Secondary — supporting accent
val Secondary = Color(0xFFE57373)

// Tertiary — additional accent
val Tertiary = Color(0xFFFFB74D)

// Background — main app background
val Background = Color(0xFFF8F9FA)

// Surface — cards, panels, containers
val Surface = Color(0xFFFFFFFF)

// Text / icons on primary
val OnPrimary = Color(0xFFFFFFFF)

// Text / icons on secondary
val OnSecondary = Color(0xFF3B0A0A)

// Text / icons on tertiary
val OnTertiary = Color(0xFF3E2723)

// Text on background
val OnBackground = Color(0xFF1C1B1F)

// Text on surfaces
val OnSurface = Color(0xFF1C1B1F)

// ================================
// ADDITIONAL ZIVA COLORS
// ================================

// SOS / Emergency
val SosRed = Color(0xFFD32F2F)
val SosDarkRed = Color(0xFFB71C1C)
val SosLightRed = Color(0xFFFFEBEE)

// Success
val SuccessGreen = Color(0xFF2E7D32)
val SuccessLightGreen = Color(0xFFE8F5E9)

// Warning
val WarningOrange = Color(0xFFEF6C00)
val WarningLightOrange = Color(0xFFFFF3E0)

// Information
val InfoBlue = Color(0xFF1976D2)
val InfoLightBlue = Color(0xFFE3F2FD)

// Neutral colors
val TextPrimary = Color(0xFF1C1B1F)
val TextSecondary = Color(0xFF6B6B6B)
val Divider = Color(0xFFE0E0E0)
val CardBackground = Color(0xFFFFFFFF)

// Disabled
val Disabled = Color(0xFFBDBDBD)
val DisabledBackground = Color(0xFFF0F0F0)